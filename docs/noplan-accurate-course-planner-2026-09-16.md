# 코스 추천 정확도 개선 — 2026-09-16

## 적용 범위

프론트 `generateCourse`와 백엔드 `/api/course/generate/generate-course`를 `requested-combinations-v2`로 연결했다. 모바일 빠른 추천·채팅 조건 확인, PC 여행 구간 추천에 적용한다. 별도의 기존 legacy 생성 엔드포인트는 이번 전환 대상이 아니다.

## 입력과 결과

- 선택한 활동을 최대 3개까지 모두 포함해야 성공한다. 심야 식사+술집도 2개 활동으로 보존한다. 산책·카페 자동 추가 및 한 활동만 남긴 부분 성공은 하지 않는다.
- 조건 확인에서 전체 1인 예산(제한 없음 포함), 정확한 인원, 주류 선호·주문량, 제외 음식 업종을 받는다. PC에서는 새로 추가할 구간의 예산이다.
- 성별·나이 대신 직접 고른 취향과 예산을 사용한다. 인원은 비용 나누기와 명확한 2인 이상 제약 등에 반영하며, 미확인 좌석 수용 인원은 보장하지 않는다.
- 로그인 사용자 기본 취향 저장은 해당 계정의 이 브라우저 localStorage에만 저장한다. 계정 서버 동기화가 아니며 인원은 매번 확인한다.
- 채팅 추출 스키마에 근거 문장을 가진 추가 활동을 넣어 복수 활동을 보존한다. 추출 실패 시 기존 단순 파서는 여전히 제한적이므로 조건 확인 화면에서 활동을 확인한다.
- 영업시간 미확인 장소는 기본 제외한다. 명시적으로 허용하면 미확인 표시를 붙인다. 휴무·폐업·품질 탈락은 허용 옵션과 무관하게 제외한다.
- 결과에 필수 활동 충족 수, 장소별·전체 1인 예상 가격, 영업 확인 상태와 이동 추정 여부를 표시한다. 미확인 가격은 0원으로 표시하지 않는다.

## 비용과 조합 탐색

기존 메뉴에서 대관·사무실·기간권·중량 단가·이상 가격 등을 제외한다. 음식은 1인분, 카페는 음료와 디저트 범위, 술집은 공유 안주와 선택 주류 주문량으로 추정한다. 가격의 25~75 분위 범위와 상단 15% 여유를 사용한다. 실제 주문 총액 보장이 아니다. 와인 등 병/잔 구분이 불명확하면 비용을 계산하지 않는다. 놀거리·산책의 비용 자료는 현재 자동 계산하지 않는다. 금액 제한이 있으면 비용 미확인 장소를 제외하고, 제한 없음에서는 미확인으로 표시한다.

활동별 상위 40개 후보를 모아 최대 40개 조합을 유지하는 제한된 조합 탐색을 수행한다. 시간·도보 거리·전체 예상 예산·세부 활동 조건을 확인한 뒤 유지된 완성 조합의 실제 이동/영업을 확인한다. 영업 검증은 최대 28회와 시간 한도 내에서 수행하며 전체 후보에 대한 최적해 보장은 아니다. 기본 영업 검증 예산은 최소 25초이며 기존 환경변수의 더 큰 값은 유지한다. 프론트 요청 제한은 55초다.

Google 도보 경로를 받으면 실제 이동 시간으로 도착 시각을 다시 계산하고 영업과 종료 시각을 재검증한다. 경로가 없으면 보수적인 직선거리 기반 도보 추정을 표시한다. 영업은 체류 전체를 확인해 중간 휴게시간도 제외하고, 먼 미래 날짜에는 이번 주 임시 영업시간을 재사용하지 않는다.

미검증 단일 교체 후보를 반환하지 않는다. 다른 장소를 원하면 조건을 바꾸거나 전체 코스를 다시 추천받아 시간·예산을 다시 확인한다.

## 배포와 검증

프론트와 백엔드 모두 배포한다. 이번 변경으로 추가할 SQL·테이블·환경변수는 없다. 기존 Google Places/Routes 설정을 사용한다. 기존 공유사무실·스터디카페 제거 및 재수집 차단 변경도 작업 트리에 함께 있으므로 누락하지 않는다. 이번 알고리즘 작업은 실제 운영 DB 추가 수정이나 대량 외부 API 수집을 수행하지 않았다.

자동 검증: 백엔드 단위·실제 라우트 핸들러 통합 테스트, 프론트 TypeScript/빌드 및 변경 파일 ESLint. 브라우저에서 360/390/1440px 가로 넘침, 입력 검증, 요청 조건 전달, 실제 새 조합 엔진을 이용한 2개 장소 결과/예산 부족 흐름을 확인했다. DB·Google 응답은 fixture로 대체했으므로 운영 API 성공률 측정은 별도다.

수동 확인 예시:

1. 성수/건대, 20:30~다음 00:30, 친구 2명, 맛집+술, 1인 5만 원: 2개 활동이 모두 포함되거나 명확한 부족 이유가 나오는지 확인.
2. 같은 조건 2만 원: 비싼 코스를 그대로 반환하지 않는지 확인.
3. 술 취향/음식 업종 제외를 바꾸어 결과 확인.
4. 카페를 선택하지 않은 심야 요청에 카페·산책이 자동 추가되지 않는지 확인.
5. 영업시간 미확인 허용을 켰을 때 미확인 표시가 유지되는지 확인.

정확한 예약·좌석, 라스트오더, 현재 메뉴 가격은 아직 보장하지 않는다. 현재 자료 부족으로 추천 실패가 늘 수 있으며, 이때 실패 메시지와 입력 조건으로 다음 보완 지점을 판단한다.

## 건대 일식·포차 예산 실패 후속 수정

사용자 조건: 지금~밤까지, 두명/연인, 일식+포차, 소주 1인 2주문, 해산물 업종 제외, 도보 짧게. 운영 로그의 checks=0 및 price_unknown=7을 확인하고 설정된 DB를 읽기 전용으로 조회했다. 소주라는 일반명 대신 `진로골드 5,000원`처럼 브랜드만 등록된 메뉴를 인식하지 못하는 문제가 있었다. 실제 저장 가격을 사용해 브랜드명 소주를 분류하도록 수정했다. 가격이 아예 없는 장소의 가격을 임의로 채우지는 않는다.

기존에는 중간 후보 하나라도 budget/price_unknown으로 탈락하면 전체 실패를 예산 탓으로 설명했다. 활동별 가격 자료 부족/예산 초과/취향 후보 부족과 조합 단계의 도보·시간·총예산 실패를 구분하도록 수정했다. 로그에 정규화된 예산·주류·제외 업종, 활동별 후보 통과 수와 조합 단계별 탈락 수를 기록한다. 실패 화면에서도 예산과 주류·제외 업종 조건을 확인할 수 있다. 출발지 placeholder는 `예: OO역, OO동`으로 수정했다.

조회한 건대 장소·메뉴 스냅샷과 근사 출발 좌표로 재현할 때 수정 후 10만 원은 가격·동선 조합을 통과했다. 3만 원은 현재 가격을 계산할 수 있는 후보 및 도보 조건의 조합이 부족했다. 이 재현은 영업 응답을 테스트 값으로 대체했으므로 실제 영업/운영 전체 성공을 뜻하지 않는다. 백엔드 292개 테스트, 프론트 빌드 및 변경 화면 ESLint 통과. 두 서버 배포가 필요하며 추가 SQL·환경변수는 없다.

### 주류 분류 공통화 후속 수정

`alcoholMenus.js`에서 소주·맥주·와인·하이볼/칵테일·기타 주류를 함께 분류한다. 일반명, 등록된 한글/영문 브랜드·칵테일 이름 별칭, 명시적 메뉴 카테고리를 사용하며 취향 필터와 비용 계산이 같은 결과를 사용한다. 카스·테라·켈리·하이네켄, 산토리/버번 위스키 하이볼, 모히또·마가리타, 샤르도네 등의 회귀 사례를 추가했다. 임의의 모든 브랜드/시그니처 이름을 인식하는 모델은 아니며 별칭과 분류 데이터는 확장할 수 있다.

무알콜 음료, 카스테라·테라스 같은 동명 단어, 주류를 재료로 쓴 음식은 술로 분류하지 않는다. 와인·위스키 등의 잔/병/용량 단위가 없으면 비용 미확인으로 유지하고 명시된 병 가격은 병 주문 가격으로 사용한다. 안주+술 세트나 묶음 주류를 한 잔 가격으로 계산하지 않는다. 실제 가격을 새로 생성하거나 DB에 기록하지 않는다.

네 가지 주류 취향과 상관없음 선택의 플래너 통합 테스트 포함, 백엔드 총 300개 테스트 통과. 이번 주류 공통화는 백엔드 배포 대상이다.

## 22:31 건대 요청의 시간·영업 검증 후속 수정

운영 로그 `0e872302-83b7-4582-9174-3c856…`의 두 요청을 구분했다. 첫 요청은 지금~밤까지(22:31~다음 00:30), 두 번째 종료 14:36은 다음 날 오후 2:36이다. 첫 요청은 기존의 숨겨진 출발 준비시간 10분과 연결 이동 고정 여유 10분 때문에 영업 조회 전에 조합이 사라졌다. 두 번째는 영업 종료/영업 미확인으로 실패했다.

- 정확도 플래너의 ‘지금’ 출발에 10분을 더하지 않는다. 다음 활동의 최소 체류 시간과 후보 사이의 이동 추정치를 이용해 잔여 시간을 계산하고 최종 선택 경로를 다시 검증한다. 식사 최소 45분·술집 최소 60분은 유지한다.
- ‘밤까지’는 가장 가까운 미래의 00:30으로 계산한다. 자정 이후 요청에 하루를 더 붙이지 않는다. 프론트 선택 버튼에 00:30을 표시하고 직접 입력한 종료 시각에는 오전/오후 안내를 표시한다.
- 후보/완성 조합 탐색 범위를 위 설명대로 확대했다. 실패 응답에 계산한 시작·종료·총분, 로그에 장소별 영업 검증 시각·체류 시간·결과를 포함한다.
- 영업 미확인으로 실패하면 사용자가 누를 수 있는 ‘영업 미확인 후보 포함해 다시 찾기’를 표시한다. 자동으로 조건을 완화하지 않으며 휴무·폐업으로 확인된 곳은 계속 제외한다.
- 실제 부탄츄 메뉴에서 라멘 9,500원 대신 카라아게/교자 추가 세트 3,500원을 식사 가격으로 세던 사례를 수정했다. 식사 주메뉴가 확인되면 저렴한 사이드·추가 세트보다 주메뉴로 추정한다.

### 실제 데이터 확인과 남은 제한

설정된 DB에 읽기 전용 트랜잭션으로 연결해 실제 후보 수집기와 Kakao 출발지 조회, Google Places 영업 조회를 실행했다. 데이터나 코스는 저장하지 않았다. 운영 로그와 같은 527개 후보를 얻었고 로그의 출발 시각과 입력 조건을 고정해 확인했다. 추가로 운영 코드와 같은 Google Routes 호출도 연결했으나 사용 가능한 경로가 반환되지 않아 최종 이동은 기존 직선거리 기반 추정으로 처리됐다. 실제 도보 경로 검증 완료를 뜻하지 않는다.

수정 후 1인 5만 원, 일식+포차, 소주 2주문, 해산물 업종 제외, 구간 도보 500m 조건에서 시간/예산 조합 4개가 영업 확인까지 진행됐다. Google 응답은 이찌방이야기 건대점 영구 폐업, 겐로쿠우동 건대직영점 22시 종료, 나머지 식당 후보 2곳 영업 미확인이었다. 따라서 **영업 미확인을 허용하지 않은 같은 조건은 여전히 실패**하며 데이터 보완이 필요하다.

미확인을 명시적으로 허용하면 부탄츄 건대점(미확인, 22:36~23:23) → 술파는이층포차(체류 전체 영업 확인, 23:29~00:29) 조합이 생성됐다. 저장 메뉴 기준 1인 예상 27,500~38,200원이며 실제 영업·주문 총액을 보장하지 않는다. 미확인 표시를 유지한다.

최종 검증: 백엔드 전체 307개 테스트 통과, 프론트 빌드 및 변경 파일 ESLint 통과. 브라우저에서는 실제 조회 결과로 만든 API fixture로 404 사유/일정 전달, 명시적 허용 버튼 클릭 후에만 조건 변경, 미확인/확인 장소 표시, 오후 14:36 안내, 360/390/1440px 가로 넘침을 확인했다. 브라우저 테스트는 실제 운영 API 연결 테스트가 아니다. 프론트·백엔드 모두 배포하며 추가 SQL·환경변수는 없다.

## 19:50 요청의 맛집 탐방 필터 오류 수정

요청 `96ff181e-ec36-40a7-be3a-b1fb365456aa`를 그대로 재현했다. 2026-09-17 19:50~다음 00:30, 건대입구역, 두명/연인, 일식+포차, `coreIntent=food_tour`, 1인 5만 원, 소주 2주문, 해산물 제외, 도보 짧게, 영업 미확인 허용 false 조건이다. 수정 전 실제 후보 수집 527개, 기본 필터 후 식당 232개, 취향 통과 0개, 영업 검증 0회로 운영 로그와 일치했다.

원인은 `맛집 탐방`이라는 방문 선호를 필수 장소 태그로 검사한 것이다. `coreIntents.js`에 필수/선호 구분을 추가하고 정확도 플래너가 구분을 사용하도록 수정했다. 맛집 탐방·가성비·대화·휴식 등의 포괄적인 목적은 태그가 확인되면 순위에 가점을 주며 태그가 없다는 이유만으로 탈락시키지 않는다. 자료로 확인하지 못한 선호는 결과의 안내에 표시한다. 일식·포차 등 세부 업종, 건강식·스포츠 등 구체적 활동, 제외 업종, 예산, 시간, 도보 제한, 영업 확인은 기존 검증을 유지한다.

수정 후 같은 실제 DB·Kakao·Google 조회로 식당 취향 통과 37개, 예산 통과 35개, 완성 조합 5개가 나왔다. 영업 미확인 허용 false 그대로 영업 검증 3회 후 다음 코스가 완성됐다. 첫 폐업 후보는 제외했다.

- 겐로쿠우동 건대직영점: 19:55~21:10, Google 영업시간으로 체류 전체 확인, 1인 예상 7,000~9,800원.
- 술파는이층포차: 21:16~23:16, Google 영업시간으로 체류 전체 확인, 1인 예상 18,000~27,200원.
- 전체 1인 예상 25,000~37,000원. Google Routes에서 사용 가능한 경로를 받지 못해 도보 268m/362m는 추정값이다. 저장 메뉴 및 외부 영업 데이터 기준이며 실제 주문액·라스트오더·좌석을 보장하지 않는다.

영업 데이터를 대체하지 않은 읽기 전용 재현이며 DB에 코스를 저장하지 않았다. 운영과 같은 전체 HTTP 저장 흐름은 별도 라우트 fixture 테스트로 확인했다. 같은 요청, 모든 선호 모드의 태그 누락/순위 반영, 세부 업종 불일치와 구체적 활동 검증을 포함해 백엔드 311개 테스트 통과. **이번 후속 수정은 백엔드만 배포/재시작**하면 된다. 프론트 실행 코드·SQL·환경변수 변경은 없다.

## 여러 코스 비교 및 소주 가격 가정

사용자가 여러 코스 비교 개발을 요청한 뒤 “소주 가격 없으면 일단 5천원으로 계산해”라고 지정했다. 따라서 소주를 선택하고 포차·포장마차·한식주점이거나 소주 메뉴가 확인된 술집에서 소주의 계산 가능한 단가가 없으면 병당 5,000원을 가정한다. 실제 소주 단가가 있으면 우선 사용하고, 주류 주문량 0이면 가정을 적용하지 않는다. 다른 주종이나 안주 가격은 임의로 채우지 않는다. DB 메뉴 가격을 변경하지 않고 예상 비용에 `assumptions`를 기록하며 결과 요약과 장소 카드에 표시한다. 1인 소주 2주문이면 가정한 주류 비용은 10,000원이다.

### 포차 데이터 조사 결과

이전 소주 가격 누락 6곳은 당나발포차 건대점, 잔포차탕 건대점, 제주올레포차 본점 숨비소리, 실내포장마차, 어오내, 오천냥포차였다. 당나발포차는 저장된 맥주·하이볼 가격은 있지만 소주 단가가 없었고, 나머지 5곳은 저장된 주류 메뉴가 없었다. 당나발포차의 기존 메뉴 수집기 재조회에서도 소주 단가가 확인되지 않았다. 이후 사용자 가격 가정 지시에 맞춰 추가 수집을 중단했다. 데이터/메뉴를 DB에 기록하지 않았다.

### 비교와 화면 동작

- 첫 성공에서 반환하지 않는다. 최대 40개 완성 조합을 기존 28회 영업 검증·시간 한도 내에서 확인하고 통과한 조합을 비교한다. 한도에 도달해도 이미 확인된 코스는 반환한다.
- 서로 다른 장소가 확인 기회를 갖도록 조합 검증 순서를 조정한다. 최종 기본 코스는 점수순, 대안은 점수와 장소 중복을 함께 고려해 최대 3개를 선정한다. 모든 장소/조합에 대한 전수 최적화는 아니다.
- 최종 평점 순위는 Google의 평점과 같은 응답의 평가 수가 함께 확인된 경우만 사용한다. 카카오 평점에 네이버 방문자 리뷰 수를 붙이지 않는다. 신뢰도 보정은 `(평점×평가수 + 3.8×20)/(평가수+20)`, 평가 수 추가 가점은 로그와 상한을 둔다. 평점 자료가 없으면 중립 점수이며 좋은 평가를 받았다고 표시하지 않는다.
- 맛집 탐방은 식당 평가 점수를 더 크게 반영하고 전체 이동 시간·예산 소진율·영업 미확인 및 가격 가정 여부를 함께 비교한다. 아직 식당 후보 사전 탐색은 기존 거리/선호 중심이고 최종 비교에 검증 평점을 사용한다.
- API `courseOptions`에 검증된 전체 코스, 요약, 비교 근거를 전달한다. `comparison`에는 확인한 조합 수·통과 수·반환 수·한도 여부·영업 미확인 제외 발생 여부를 포함한다. 가격 자료가 부족한 장소는 `priceUnknownPlaces`로 별도 전달하며 예산 초과나 검증된 코스로 표시하지 않는다.
- 결과 화면에서 코스를 선택하면 장소·예상 비용·종료 시각·주변 행사·저장/출발 대상이 함께 변경된다. 대안을 선택하면 기본 코스의 검색 이력 ID를 재사용하지 않는다. 최초 검색 이력은 기본 코스 한 건만 기록하고 명시적 저장은 현재 선택 내용으로 수행한다.

### 실제 재현과 검증

동일 건대 요청(2026-09-17 19:50, 일식+포차, 1인 5만 원, 소주 2주문, 해산물 업종 제외, 도보 500m, 영업 미확인 불허)에서 실제 DB와 Google 조회를 재실행했다. 포차 가격 통과는 1→7곳, 거리/시간/예산 조합은 5→18개로 늘었다. 영업 확인 결과 성공은 1개(겐로쿠우동→술파는이층포차)였다. 당나발포차·잔포차탕 등을 포함한 영업 미확인 후보 때문에 3개가 채워지지 않았다. 영업 조건을 자동 완화하지 않는다. Google 도보 경로는 응답이 없어 추정 거리/시간을 사용했다.

백엔드 317개 테스트 통과. 프론트 변경 파일 ESLint·TypeScript·빌드 통과. 브라우저에서는 테스트 장소와 실제 조합 엔진으로 3개 결과를 만들어 API fixture로 전달하고 360/390/1440px 가로 넘침, 코스 전환, 소주 가정 표시, 저장 payload, 출발 화면의 선택 코스 유지를 확인했다. 브라우저 테스트는 운영 저장을 수행하지 않았다.

배포 대상: **프론트와 백엔드 모두**. 추가 SQL·테이블·환경변수 없음. 운영 배포는 수행하지 않았다.

## 빈 조건 재시도와 잘못된 지역 안내 수정

사용자 화면에는 출발지·시간·동행·활동이 기본 대체 문구로 표시되는데 모든 입력에 체크가 켜지고, 오류는 ‘현재 성수·홍대 지역만 지원합니다’로 나왔다. 코드에서 입력이 React 메모리에만 저장되어 새로고침 시 사라지는 경로, 결과/실패 화면의 재시도가 필수 입력 검사 없이 요청하는 경로, 빈 출발지에 오래된 지역 오류를 반환하는 분기를 확인했다. 사용자가 그 화면에 도달한 구체적인 조작 순서는 확인되지 않았다.

- 조건과 현재 위치 좌표를 같은 탭의 sessionStorage에 12시간 동안 보존한다. 깨졌거나 만료된 저장값은 기본 입력 화면으로 복구한다. 추천 결과나 Google 응답은 저장하지 않는다.
- 검색/실패 URL을 새로 열거나 새로고침해 진행 중 요청이 없는 경우 조건 확인 화면으로 이동한다. 자동 재요청하지 않는다. `runSearch`에서 모든 진입 경로의 필수 입력과 예산·인원을 검사해 빈 요청을 막는다.
- 백엔드는 입력 누락(`missing_conditions`), 출발지 해석 실패(`location_unresolved`), 지원 지역 밖(`unsupported_region`)을 구분한다. 지원 지역 안내를 서울로 수정했다. 실제 지역 판별 범위를 축소하지 않았다.
- 시간만 지나면 켜지던 완료 체크와 단계별 검증 완료처럼 보이는 문구를 제거했다. 입력 내용과 실제 검색 대기/실패 상태만 표시한다. 실패 시 진행 중 문구와 로딩 애니메이션을 멈춘다.

검증: 백엔드 320개 테스트, 프론트 변경 파일 ESLint·빌드 통과. 브라우저에서 빈 검색 URL 진입, 빈 결과 화면 재시도의 API 호출 차단, 조건·예산·주류의 새로고침 복원, 실패 화면 새로고침 후 동일 조건 재요청, 손상된 저장값 복구, 거짓 완료 표시 제거를 확인했다. 기존 3개 코스 비교·선택·저장/출발 흐름도 회귀 확인했다. 브라우저 API 응답은 fixture이며 운영 환경에서 사용자의 조작 순서를 직접 관찰한 것은 아니다.

이번 수정 배포 대상도 프론트와 백엔드 모두이며 추가 SQL·환경변수는 없다.

## 포차를 상호/단일 저장 분류로 제한하던 오류 수정

기존 추천은 이름/업종의 `포차|포장마차` 문구 또는 저장 `detailType=포차`에 의존했다. 건대 출발지 주변 카탈로그 술집 84개 중 기본 품질 등 필터를 통과한 59개에서 7개만 포차 후보로 남았다. 이 숫자는 건대의 실제 술집 수나 전체 영업 가능한 술집 수가 아니다. DB에 조선주막·장미술상·한사바리 등이 `호프,요리주점 > 펍`으로 저장돼 있어 포차 선택 시 누락되는 것을 확인했다.

노플랜의 포차 선택을 ‘소주와 한식 안주를 즐기는 일반 주점’이라는 이용 유형으로 정의했다. `drinkExperience.js`의 공통 판정을 후보 수집, 필수 활동 필터, 소주 5천원 가정에 함께 사용한다. 명시적 포차/포장마차 업종은 유지하고, 요리주점·일반 술집 등은 찌개·전골·닭발·두부김치 등 저장된 한식 안주 메뉴를 함께 확인해 포함한다. 업종 근거가 약하면 소주 메뉴와 한식 안주 메뉴가 함께 있어야 한다. 상호에 포차라는 말만 있다고 통과시키지 않으며 와인바·칵테일바·위스키바·일본식주점은 이 유형에서 제외한다. 저장 분류를 일괄 덮어쓰지 않는다.

실제 데이터 재현에서 포차 이용 유형 후보는 7→21개, 개별 예산 통과는 20개가 됐다. 이전 요청(2026-09-17 19:50~00:30, 두명 연인, 일식+포차, 1인 5만원, 소주 2주문, 해산물 업종 제외, 도보 500m, 영업 미확인 불허)을 읽기 전용 DB와 실제 Google 조회로 확인했다. 최대 40개 조합 확인, 영업 검증 15회, 완성 코스 4개 중 3개 반환:

- 겐로쿠우동 → 아리계곡 건대점: 1인 추정 상한 33,000원.
- 겐로쿠우동 → 장미술상 건대점: 1인 추정 상한 36,400원.
- 겐로쿠우동 → 술파는이층포차: 1인 추정 상한 37,000원.

반환 장소 모두 해당 체류 시간의 Google 영업시간 확인을 통과했다. 실제 도보 경로는 여전히 응답이 없어 추정으로 표시된다. 음식 취향 제외도 확인하면서 `일식`으로 저장된 참치 전문점이 해산물 제외를 우회하는 경우를 막았다. 일반 음식점에 해산물 메뉴가 하나 있다는 이유로 전체를 제외하는 방식은 아니다.

325개 백엔드 테스트 통과. 이번 분류 수정 자체는 백엔드 배포 대상이며 추가 SQL·환경변수는 없다. 이전 입력 보존/화면 수정의 프론트 작업은 그대로 유지했다.
